<Window x:Class="SaniStock.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SaniStock — Sign in" Height="520" Width="820"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize" Icon="/Assets/app.ico"
        Background="{StaticResource CanvasBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Brand panel -->
        <Border Grid.Column="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{StaticResource BrandColor}" Offset="0" />
                    <GradientStop Color="{StaticResource BrandDarkColor}" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel VerticalAlignment="Center" Margin="36">
                <TextBlock Text="🏺" FontSize="56" />
                <TextBlock Text="SaniStock" FontSize="34" FontWeight="Bold" Foreground="White" Margin="0,12,0,0" />
                <TextBlock Text="Production &amp; Stock Management" Foreground="#DCE7F7" FontSize="15" />
                <TextBlock Text="for Sanitary Ware Manufacturing" Foreground="#BBD0EE" FontSize="13" Margin="0,2,0,0" />
            </StackPanel>
        </Border>

        <!-- Login form -->
        <Border Grid.Column="1" Style="{StaticResource Card}" Margin="40" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Welcome back" Style="{StaticResource H1}" />
                <TextBlock Text="Sign in to continue" Style="{StaticResource Muted}" Margin="0,0,0,20" />

                <TextBlock Text="Username" Style="{StaticResource FieldLabel}" />
                <TextBox x:Name="UsernameBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,14" />

                <TextBlock Text="Password" Style="{StaticResource FieldLabel}" />
                <PasswordBox x:Name="PasswordBox" Margin="0,0,0,6"
                             KeyDown="PasswordBox_KeyDown" />

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                           FontSize="12" Margin="0,6,0,0"
                           Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />

                <Button Content="Sign in" Style="{StaticResource PrimaryButton}" Margin="0,18,0,0"
                        Height="42" Click="SignIn_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
